1. Input类：
	负责从指定的输入文件中读取文本行，并将每一行存储在一个列表中。
2. Shift类：
	接收来自Input类的行文本列表，对每一行进行单词移位处理，并将结果存储在一个新的列表中。
3. Alphabetizer类：
	接收来自Shift类的移位后的文本列表，对这些文本进行字典排序。
4. Output类：
	接收来自Alphabetizer类的排序后的文本列表，并将它们写入到指定的输出文件中。
5. Main类：
	程序的入口点，创建上述类的实例并调用它们的方法来执行整个KWIC处理流程。